{
  "name": "restaurant-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc",
    "start": "npm run compile && ts-node-dev ./",
    "prepare": "npm install husky && husky install",
    "pretty": "prettier --write \"*/**/*.{js, ts}\"",
    "lint-staged": "npm run pretty && git add -A"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.10.2",
    "husky": "^7.0.0",
    "nodemon": "^2.0.13",
    "prettier": "2.5.0",
    "ts-node": "^10.2.1",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.3"
  }
}
